# Control Plane Truth Map

Updated: 2026-03-15
Owner: Terminal 1
Scope: Group 1 from `07_codex_next_steps.md`

## Layer Map

| Layer | FACT | INFERENCE | RISK | REQUIRED ACTION |
| --- | --- | --- | --- | --- |
| Product surface | The product shell calls Supabase Edge Functions directly from the frontend. Representative callers are `useGeoSearch` -> `google-maps-search` / `web-analyzer` / `ai-qualifier` (`/Users/rotech/OCULOPS-OS/src/hooks/useGeoSearch.js:43-85`), `useAutomation` / `src/lib/automation.js` -> `automation-runner` (`/Users/rotech/OCULOPS-OS/src/lib/automation.js:12-48`), `useConversations` -> `messaging-dispatch` (`/Users/rotech/OCULOPS-OS/src/hooks/useConversations.js:117-139`), `useConnectorProxy` -> `api-proxy` (`/Users/rotech/OCULOPS-OS/src/hooks/useConnectorProxy.js:57-87`), `CommandCenter` -> `td-bridge` / `td-api` (`/Users/rotech/OCULOPS-OS/src/components/modules/CommandCenter.jsx:17-37`). Repo search returned no `src/` caller for `control-plane` or `check-and-route`. | The real product control plane is still `frontend -> direct edge function fan-out`, not `frontend -> governor -> tool bus`. | Privileged behavior is fragmented across many direct entrypoints, so audit, approval, and policy enforcement are inconsistent. | Make `control-plane` the default entrypoint for privileged sends, runtime commands, agent launches, and write-capable connector execution. |
| Governed edge path | The governor path is real inside Supabase. `control-plane` action `tool_dispatch` runs governance checks and adds `via_control_plane` / `via_tool_bus` routing metadata before invoking `invokeToolThroughBus()` (`/Users/rotech/OCULOPS-OS/supabase/functions/_shared/orchestrator-core.ts:448-523`). `api-proxy` blocks high-risk legacy direct calls without control-plane + tool-bus evidence (`/Users/rotech/OCULOPS-OS/supabase/functions/api-proxy/index.ts:502-537`). `messaging-dispatch` does the same for outbound messaging (`/Users/rotech/OCULOPS-OS/supabase/functions/messaging-dispatch/index.ts:365-396`). | The governor exists as an enforcement island, not as the default product entrypoint. | Product callers that bypass `control-plane` can still reach ungated direct edges, or get blocked only after they are already on the wrong path. | Move caller selection up to the product layer so privileged actions enter through `control-plane` first instead of relying on downstream rejection. |
| Messaging and connector execution | Automation `send_live` already routes through `control-plane` -> `tool-bus` -> `messaging-dispatch` (`/Users/rotech/OCULOPS-OS/supabase/functions/_shared/automation.ts:540-600`). `agent-outreach` send actions also route through `invokeToolThroughBus()` (`/Users/rotech/OCULOPS-OS/supabase/functions/agent-outreach/index.ts:337-356`). | The intended governed path is already implemented for the exact operations that are highest risk. | The remaining problem is caller drift, not missing infrastructure. | Reuse the existing tool-bus route for all live sends and write-capable connectors from the UI. |
| Runtime bridge surfaces | The product still treats the Mac Mini runtime as browser-reachable. `useReadiness` probes `http://127.0.0.1:38791/health` directly (`/Users/rotech/OCULOPS-OS/src/hooks/useReadiness.js:12-18`). `CommandCenter` probes `localhost` n8n, Agent Zero, and ComfyUI health endpoints from the browser (`/Users/rotech/OCULOPS-OS/src/components/modules/CommandCenter.jsx:40-60,143-159`; `/Users/rotech/OCULOPS-OS/src/data/n8nAirdropIntel.js:56-80`). Terminal 2 approved only `http://127.0.0.1:38793/api/v1/health`, `http://127.0.0.1:38791/health`, `http://127.0.0.1:38791/api/readiness`, and the public Supabase readiness mirror for current runtime dependencies (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:64-77`). | Runtime readiness is still coupled to a same-machine browser session, not to the approved runtime health and readiness surfaces. | Remote or Vercel-served sessions can show false red or false green because localhost reachability is not a valid cross-environment signal. | Replace browser localhost probes with an edge-backed status view sourced from the canonical readiness artifact and its API projection at `http://127.0.0.1:38791/api/readiness`; use `http://127.0.0.1:38793/api/v1/health` only as approved gateway liveness. |
| TouchDesigner bridge plane | TouchDesigner bridge reads go through `td-bridge` and `td-api`, but command execution still calls `agent-${codeName}` directly with the service role key (`/Users/rotech/OCULOPS-OS/supabase/functions/td-command/index.ts:81-99`). | The bridge plane is partly integrated and partly bypassing governor identity. | Runtime-originated agent commands can skip control-plane policy and trace normalization. | Route TouchDesigner command execution through `control-plane` or a governor-owned agent launcher instead of direct `agent-*` invocation. |
| Local runtime estate | Runtime source files show PM2-managed services at `n8n=5680`, `dashboard_api=38791`, `api_gateway=38793`, `chains=38804`, `voice=38805` (`/Users/rotech/AGENCY_OS/ecosystem.config.js:17-75`). Terminal 2 approved only `38793/api/v1/health`, `38791/health`, `38791/api/readiness`, and the public readiness mirror for Group 2 dependencies (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:64-77`). Terminal 2 also marked Docker `n8n :5678` as a duplicate runtime (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:79-97`). | The runtime already has a canonical health/readiness set, but the product shell still mixes it with stale localhost assumptions. | The frontend default for n8n is `5678`, while PM2 declares `5680`, so health status can be wrong even on the operator machine. | Replace product-surface runtime defaults with the approved endpoint set and remove stale `5678` / `5679` assumptions from the UI. |
| Workflow runtime and unwired surfaces | Terminal 3 confirmed native `n8n :5680` behind gateway `:38793` as the real workflow target and published the retained caller-facing webhook surfaces, while noting no active workflow names match `CORTEX` or `ATLAS` in the registry (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:112-117`). In product code, `launch_n8n` still posts directly to a resolved webhook target (`/Users/rotech/OCULOPS-OS/supabase/functions/_shared/automation.ts:896-940`). `check-and-route` exists as a tunnel-based bridge to the Mac Mini runtime (`/Users/rotech/OCULOPS-OS/supabase/functions/check-and-route/index.ts`), but there is no current product-surface caller. | The live workflow plane is real, but naming and caller selection still drift between direct webhook targets, registry labels, and unused bridge surfaces. | Direct workflow triggers can bypass a unified governor trace, while dead bridge surfaces increase ambiguity about what is authoritative. | Normalize workflow launches against the retained `n8n :5680` surfaces proven by Terminal 3, and classify `check-and-route` as legacy unless a live caller is proven. |
| Runtime entrypoint safety | Terminal 2 reported that tested protected routes on `38793` are effectively unauthenticated, `POST /api/v1/siri` is public, `38792` has open mutating routes, `40000` / `40001` / `40002` are live but undeclared, `127.0.0.1:5679` is not reliable, and current tunnel URLs are unstable (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:79-97`). | Only a narrow set of runtime surfaces can be treated as approved truth for this audit cycle. | If product logic treats undeclared or unauthenticated runtime surfaces as canonical, the control-plane map will overstate actual governance. | Keep product and audit references constrained to the approved endpoint set; do not route privileged actions through `38792`, undeclared OMNICENTER ports, or tunnel URLs. |

## Privileged Action Classification

| Action family | FACT | INFERENCE | RISK | REQUIRED ACTION |
| --- | --- | --- | --- | --- |
| Prospecting reads | `google-maps-search`, `web-analyzer`, `ai-qualifier`, `market-data`, and `social-signals` are called directly from the product shell as edge functions (`/Users/rotech/OCULOPS-OS/src/hooks/useGeoSearch.js:43-85`; `/Users/rotech/OCULOPS-OS/src/hooks/useMarkets.js:310-326`; `/Users/rotech/OCULOPS-OS/src/hooks/useSocialSignals.js:183-199`). | These are read-heavy product capabilities, not the core governor gap. | They still bypass a single governor identity, but they do not create the same immediate provider-write risk as messaging or runtime command paths. | Retain as direct edge adapters for now, but do not label them as governor-routed. |
| Messaging live send from Messaging UI | The Messaging UI stages a draft and then calls `messaging-dispatch` directly (`/Users/rotech/OCULOPS-OS/src/components/modules/Messaging.jsx:176-186`; `/Users/rotech/OCULOPS-OS/src/hooks/useConversations.js:117-139`). `messaging-dispatch` defaults missing risk metadata to `high` and missing source to `manual` (`/Users/rotech/OCULOPS-OS/supabase/functions/messaging-dispatch/index.ts:37-65`), then blocks high-risk legacy sends unless they arrived through control-plane + tool-bus (`/Users/rotech/OCULOPS-OS/supabase/functions/messaging-dispatch/index.ts:352-396`). | The current UI live-send path is missing its governor caller and is likely blocked by design. | The primary messaging surface can fail or drift into ad hoc workarounds. | Replace the direct caller with `control-plane` `tool_dispatch` to `messaging-dispatch`, or route the UI through `agent-outreach`. |
| Automation live send | Automation send-live uses the governed path already (`/Users/rotech/OCULOPS-OS/supabase/functions/_shared/automation.ts:540-600`). | This is the best current example of the intended control plane. | If the UI keeps a parallel direct-send path, behavior will diverge between automation and operator actions. | Reuse the automation caller pattern as the canonical send path for the product surface. |
| Outreach queue send | `agent-outreach` sends through `invokeToolThroughBus()` (`/Users/rotech/OCULOPS-OS/supabase/functions/agent-outreach/index.ts:337-356`), while approval and queue management reach `agent-outreach` directly from the UI (`/Users/rotech/OCULOPS-OS/src/hooks/useApprovals.js:65-86`; `/Users/rotech/OCULOPS-OS/src/hooks/useOutreachQueue.js:51-67`). | The send path is governed; the surrounding queue actions are not yet normalized through one governor entrypoint. | Audit and approval events can span both governed and unguided call chains for the same operator flow. | Keep `agent-outreach` send routing, but decide with Terminal 4 whether approval/queue actions also need a single governor-owned launcher. |
| Connector execution from UI | The UI calls `api-proxy` directly (`/Users/rotech/OCULOPS-OS/src/hooks/useConnectorProxy.js:57-87`). `api-proxy` defaults missing risk to `medium` and missing source to `manual` (`/Users/rotech/OCULOPS-OS/supabase/functions/api-proxy/index.ts:72-97`), and only enforces tool-bus evidence when the request is explicitly high risk (`/Users/rotech/OCULOPS-OS/supabase/functions/api-proxy/index.ts:502-537`). | Read-only connector use can work directly, but write-capable connector calls can bypass governor classification unless the caller sets the right metadata first. | Sensitive connector calls can remain outside the governor path simply because the frontend did not label them as high risk. | Route all write-capable or credential-sensitive connector actions through `control-plane`; keep direct `api-proxy` only for clearly read-only health or lookup cases. |
| Direct agent launches from product surface | The product directly launches `agent-cortex` (`/Users/rotech/OCULOPS-OS/src/components/modules/GTM.jsx:108-145`), `agent-herald` (`/Users/rotech/OCULOPS-OS/src/components/modules/HeraldAgent.jsx:43-51`), `agent-runner` (`/Users/rotech/OCULOPS-OS/src/hooks/useAgentVault.js:103-123`), `agent-copilot` (`/Users/rotech/OCULOPS-OS/src/components/ui/CopilotPanel.jsx:24-63`), `agent-studies` (`/Users/rotech/OCULOPS-OS/src/hooks/useAgentStudies.js:13-31`), and `agent-forge` (`/Users/rotech/OCULOPS-OS/src/hooks/useGenerativeMedia.js:50-66`). | Agent orchestration is still many-entrypoint, not governor-singular. | Identity, approvals, and model-routing policy can drift between agent entrypoints. | Consolidate agent launch under one governor-owned launcher after Terminal 4 confirms the canonical agent/governor identity. |
| Runtime health and runtime commands | `useReadiness` and `CommandCenter` both assume browser access to localhost runtime services (`/Users/rotech/OCULOPS-OS/src/hooks/useReadiness.js:12-18`; `/Users/rotech/OCULOPS-OS/src/components/modules/CommandCenter.jsx:50-60,143-159`). `td-command` invokes agents directly with service-role auth (`/Users/rotech/OCULOPS-OS/supabase/functions/td-command/index.ts:81-99`). Terminal 2 approved `38793/api/v1/health`, `38791/health`, and `38791/api/readiness`, and separately cautioned that tested protected `38793` routes are effectively unauthenticated (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:64-77`; `/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:79-97`). | The runtime plane is not yet collapsed behind one canonical, governed caller path for product and TouchDesigner callers. | Runtime command and readiness truth can split by environment and by caller type, and gateway liveness does not yet imply authenticated control-plane enforcement. | Use `38791/api/readiness` as the canonical readiness projection, keep `38793/api/v1/health` as gateway liveness only, and reroute runtime commands through a governor-owned launcher instead of direct agent calls. |
| Workflow launch from automation | `automation-runner` can execute `launch_n8n`, and that step posts directly to the resolved webhook target (`/Users/rotech/OCULOPS-OS/supabase/functions/_shared/automation.ts:896-940`). Terminal 3 confirmed the retained caller-facing workflow surfaces on native `n8n :5680` behind gateway `:38793` (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:112-117`). | Automation workflow execution still keeps a direct runtime webhook path outside the governor envelope. | Workflow launches can drift away from the same control-plane trace and runtime normalization expected for privileged sends. | Keep the retained workflow IDs from Terminal 3 as the current source of truth and plan a governor-owned workflow launcher if these launches remain part of the privileged control plane. |

## Runtime Assumptions Reconciled Against Runtime Source

| Surface | FACT | INFERENCE | RISK | REQUIRED ACTION |
| --- | --- | --- | --- | --- |
| Canonical readiness artifact | Terminal 2 identified `/Users/rotech/AGENCY_OS/CONTEXT/ecosystem-readiness.latest.json` as the canonical machine-readable readiness artifact, `http://127.0.0.1:38791/api/readiness` as its canonical API projection, and the public Supabase JSON as a derived mirror only (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:99-110`). | The audit now has one machine-readable readiness source instead of competing runtime health claims. | Product code is still not reading from that source. | Back product-surface readiness from the canonical artifact or its approved API projection, not from browser localhost probes. |
| Dashboard API | Health endpoint is `http://127.0.0.1:38791/health` (`/Users/rotech/AGENCY_OS/RUNNERS/dashboard/dashboard_api.py:333-338`), and Terminal 2 approved it as a valid raw runtime endpoint (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:64-77`). | `useReadiness` is tied to a real runtime endpoint, not an invented one. | It is still a browser-local dependency and is not the canonical machine-readable readiness projection. | Keep `/health` as raw dashboard liveness; move product readiness logic to `38791/api/readiness` or the approved mirror. |
| Runtime gateway | Runtime gateway is `http://127.0.0.1:38793/api/v1/*` and already proxies readiness/governance (`/Users/rotech/AGENCY_OS/MCP/oculops-mcp-server/api-gateway.js:72-140`). Terminal 2 approved `http://127.0.0.1:38793/api/v1/health` but warned that tested protected routes on `38793` are effectively unauthenticated (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:64-97`). | `38793` is an approved gateway liveness surface, not yet a trustworthy authenticated control plane for privileged product calls. | Treating gateway reachability as governance proof would overstate the current runtime hardening. | Use `38793/api/v1/health` only for gateway liveness until Terminal 2 hardening claims change. |
| n8n | PM2 declares `N8N_PORT: 5680` (`/Users/rotech/AGENCY_OS/ecosystem.config.js:18-28`). Terminal 2 marked Docker `n8n :5678` as a duplicate runtime (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:79-97`). Terminal 3 confirmed native `n8n :5680` behind gateway `:38793` as the real workflow target (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:112-117`). `CommandCenter` still defaults to `http://localhost:5678` (`/Users/rotech/OCULOPS-OS/src/data/n8nAirdropIntel.js:56-64`). | There is a confirmed port and runtime mismatch between product assumptions and the retained workflow runtime. | CommandCenter can report false offline or point operators at the duplicate Docker runtime. | Remove `5678` from product defaults and align workflow/runtime references to native `5680` behind the approved gateway layer. |
| Agent Zero and ComfyUI | Product defaults are `50080` and `8188` (`/Users/rotech/OCULOPS-OS/src/data/n8nAirdropIntel.js:65-80`). PM2 confirms neither service in `ecosystem.config.js`, and Terminal 2 did not include them in the approved endpoint set (`/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:64-77`). | These remain product assumptions, not approved runtime truth for this audit cycle. | Product status can imply runtime ownership that the current runtime manifest does not prove. | Leave them out of canonical readiness claims until Terminal 2 publishes them as approved surfaces. |
