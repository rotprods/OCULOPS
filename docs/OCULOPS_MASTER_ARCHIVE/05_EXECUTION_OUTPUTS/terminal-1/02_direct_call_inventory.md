# Direct Call Inventory

Updated: 2026-03-15
Owner: Terminal 1

## Product Surface and Runtime Callers

| Caller | Direct callee | Evidence | FACT | INFERENCE | RISK | REQUIRED ACTION |
| --- | --- | --- | --- | --- | --- | --- |
| `useReadiness` | `http://127.0.0.1:38791/health` | `/Users/rotech/OCULOPS-OS/src/hooks/useReadiness.js:12-18`; `/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:64-77,99-110` | Browser code hits the dashboard API health endpoint directly even though Terminal 2 identified `38791/api/readiness` and the readiness artifact as canonical machine-readable readiness. | The product shell assumes same-machine runtime reachability instead of using the approved readiness projection. | Readiness becomes environment-specific instead of control-plane-specific. | Reroute through an edge-backed view sourced from `http://127.0.0.1:38791/api/readiness` or the approved public mirror; keep `38791/health` only as raw runtime liveness. |
| `CommandCenter.refreshStack()` | `http://localhost:5678/healthz`, `http://localhost:50080/`, `http://localhost:8188/system_stats` by default | `/Users/rotech/OCULOPS-OS/src/components/modules/CommandCenter.jsx:143-159`; `/Users/rotech/OCULOPS-OS/src/data/n8nAirdropIntel.js:56-80`; `/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:79-97,112-117` | Browser code probes runtime services directly and hardcodes default ports. Terminal 2 marked Docker `5678` as a duplicate runtime, and Terminal 3 confirmed native `n8n :5680` behind gateway `:38793` as the real workflow target. | This is a direct runtime inventory embedded in the UI, not a governed status service. | Product state can drift from real PM2/runtime truth and can point operators at the duplicate Docker runtime. | Reroute through `td-api` or an approved readiness/runtime manifest surface, and remove `5678` / `5679` assumptions from the UI. |
| Messaging live send | `messaging-dispatch` direct from the UI | `/Users/rotech/OCULOPS-OS/src/hooks/useConversations.js:117-139`; `/Users/rotech/OCULOPS-OS/src/components/modules/Messaging.jsx:176-186` | The UI calls the provider-send edge directly instead of `control-plane`. | This is the missing governor caller in the main outbound messaging path. | `messaging-dispatch` defaults missing calls to `high` + `manual` and can block the UI path (`/Users/rotech/OCULOPS-OS/supabase/functions/messaging-dispatch/index.ts:37-65,352-396`). | Reroute through `control-plane` `tool_dispatch` to `messaging-dispatch`, or replace the UI send entrypoint with `agent-outreach`. |
| Messaging manual draft open | Gmail, WhatsApp, LinkedIn, Instagram deep links | `/Users/rotech/OCULOPS-OS/src/lib/outreach.js:36-112`; `/Users/rotech/OCULOPS-OS/src/components/modules/Messaging.jsx:157-170` | The product can open provider URLs directly from the browser. | These are manual fallback paths, not governed provider delivery. | Teams can mistake deep-link launch paths for real governed messaging. | Keep only as explicit manual fallback; remove them from any core-send or readiness claim. |
| GTM enrichment | `agent-cortex` direct with anon key | `/Users/rotech/OCULOPS-OS/src/components/modules/GTM.jsx:108-145` | GTM calls an agent edge directly instead of a governor-owned launcher. | The product still has direct privileged agent entrypoints. | Audit and policy behavior can diverge from other agent entrypaths. | Reroute through `control-plane` or a canonical authenticated agent launcher after Terminal 4 settles governor identity. |
| Herald briefing trigger | `agent-herald` direct with anon key | `/Users/rotech/OCULOPS-OS/src/components/modules/HeraldAgent.jsx:43-51` | The UI can trigger a Telegram-sending agent directly. | This is a privileged provider-write path outside the governor entrypoint. | Telegram delivery can bypass a single approval and audit path. | Reroute through a governor-owned agent launcher; keep `agent-herald` as an internal callee, not the primary product entrypoint. |
| Vault / Marketplace run agent | `agent-runner` | `/Users/rotech/OCULOPS-OS/src/hooks/useAgentVault.js:103-123` | The UI launches arbitrary vault agents directly. | `agent-runner` has internal governance (`/Users/rotech/OCULOPS-OS/supabase/functions/agent-runner/index.ts:15-18,84-97`), but the product still bypasses a single governor facade. | Multiple agent launch surfaces can fragment policy and model-routing behavior. | Consolidate behind one canonical governor-run agent launcher when Terminal 4 finalizes the governor identity. |
| Approvals and outreach queue | `agent-outreach` direct | `/Users/rotech/OCULOPS-OS/src/hooks/useApprovals.js:65-86`; `/Users/rotech/OCULOPS-OS/src/hooks/useOutreachQueue.js:51-67` | Queue and approval actions reach `agent-outreach` directly. | The queue shell is not governor-first, even though the send subpath is. | Mixed control paths make queue state harder to audit end to end. | Retain short term; review with Terminal 4 whether approval actions also need a single governor entrypoint. |
| Connector proxy | `api-proxy` direct | `/Users/rotech/OCULOPS-OS/src/hooks/useConnectorProxy.js:57-87` | The UI calls the connector proxy directly. | This is acceptable for read-only connector use but not for high-risk connector writes. | Write-capable connector calls can stay unguided if the caller never marks them as high risk. | Route privileged connector actions through `control-plane`; keep direct `api-proxy` only for low-risk read/health flows. |
| Automation runner | `automation-runner` direct | `/Users/rotech/OCULOPS-OS/src/lib/automation.js:12-48` | The automation shell launches workflows directly. | This is acceptable because `automation-runner` itself routes live sends through governor when needed. | Product and automation can still diverge if the UI keeps separate direct-send paths. | Retain for workflow execution; reuse the automation send-live pattern as the canonical governed messaging caller. |
| `launch_n8n` workflow step | Resolved `n8n` webhook target direct | `/Users/rotech/OCULOPS-OS/supabase/functions/_shared/automation.ts:896-940`; `/Users/rotech/OCULOPS_MASTER/05_EXECUTION_OUTPUTS/_shared/HANDOFFS.md:112-117` | Automation workflow execution still posts directly to the resolved webhook target. Terminal 3 confirmed the retained caller-facing workflow surfaces on native `n8n :5680` behind gateway `:38793`. | Workflow launch is a real direct runtime call outside the governor envelope. | Workflow execution can drift from the same control-plane trace and runtime normalization expected for privileged sends. | Keep Terminal 3's retained workflow surfaces as the current truth and plan a governor-owned workflow launcher if `launch_n8n` remains a privileged path. |
| TouchDesigner command receiver | `agent-${codeName}` direct with service role | `/Users/rotech/OCULOPS-OS/supabase/functions/td-command/index.ts:81-99` | Runtime-originated commands bypass `control-plane` and call agent edges directly. | The runtime bridge is not yet governor-singular. | Service-role runtime commands can skip the same policy envelope expected by the product shell. | Reroute `td-command` through `control-plane` or a governor-owned agent launcher. |

## Edge Provider and Model Callers

| Edge/provider family | Evidence | FACT | INFERENCE | RISK | REQUIRED ACTION |
| --- | --- | --- | --- | --- | --- |
| Governed provider send adapters | `/Users/rotech/OCULOPS-OS/supabase/functions/_shared/gmail.ts:140-170`; `/Users/rotech/OCULOPS-OS/supabase/functions/_shared/whatsapp.ts:45-79` | Gmail and WhatsApp provider calls are real direct provider adapters at the edge layer. | These are the correct terminal provider adapters, but only when the caller reached them through a governed path. | If product callers bypass the governor, the adapter layer cannot restore caller intent or approval context by itself. | Retain the adapters; reroute the caller layer, not the adapter implementation. |
| Telegram outbound from Herald | `/Users/rotech/OCULOPS-OS/supabase/functions/agent-herald/index.ts:114-179`; `/Users/rotech/OCULOPS-OS/supabase/functions/_shared/telegram.ts:79-96` | `agent-herald` combines external feed lookups with direct Telegram send behavior. | Herald is acting as both governor-facing agent and terminal provider adapter. | Product-triggered Telegram sends bypass a single governor facade. | Route Herald invocation through the governor; keep Telegram send in the terminal callee. |
| Direct model-provider calls in general-purpose edges | `/Users/rotech/OCULOPS-OS/supabase/functions/ai-advisor/index.ts:191-205`; `/Users/rotech/OCULOPS-OS/supabase/functions/knowledge-embed/index.ts:27-40`; `/Users/rotech/OCULOPS-OS/supabase/functions/agent-copilot/index.ts:1210`; `/Users/rotech/OCULOPS-OS/supabase/functions/_shared/agent-brain-v2.ts:65,194` | Multiple UI-facing and shared agent surfaces call OpenAI directly today. | There is not yet one canonical model/governor router. | Model policy, locality rules, and billing visibility can drift across edges. | Hand off to Terminal 4 for consolidation under the canonical governor/model-routing identity. |
| Read-only provider ingest edges | `/Users/rotech/OCULOPS-OS/supabase/functions/google-maps-search/index.ts:71-76,278-317`; `/Users/rotech/OCULOPS-OS/supabase/functions/market-data/index.ts:115-120,155-162,201-208`; `/Users/rotech/OCULOPS-OS/supabase/functions/social-signals/index.ts:188-214,277-305,364-369`; `/Users/rotech/OCULOPS-OS/supabase/functions/meta-business-discovery/index.ts:35-46` | Google Maps, market data, Reddit/Apify/HN, and Meta discovery are direct provider reads from edge functions. | These are normal read adapters and are not the main governor breach. | They still bypass a single runtime/provider registry if governor identity later expects central mediation. | Retain as direct edge adapters for now; revisit only if Terminal 4 requires a unified provider router for all third-party reads. |
| Direct execution/sync providers | `/Users/rotech/OCULOPS-OS/supabase/functions/manychat-sync/index.ts:35-75`; `/Users/rotech/OCULOPS-OS/supabase/functions/send-email/index.ts:22`; `/Users/rotech/OCULOPS-OS/supabase/functions/welcome-email/index.ts:118` | Some execution surfaces still call providers directly without a governor-owned caller envelope. | These are secondary privileged paths outside the core messaging/governor model. | Future product surfaces can reuse these as shortcuts and recreate governance drift. | Mark as non-core until they are either wrapped by governor entrypoints or explicitly retired. |
